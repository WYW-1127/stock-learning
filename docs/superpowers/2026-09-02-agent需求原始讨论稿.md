我看完了。**这份设计已经基本符合你刚才描述的目标，而且 Agent 方向是对的。**尤其是 S2/S3/S5/S7，把“自然语言 → 查用户真实数据 → 再分析”定义得比较清楚。

不过如果你准备**现在就交给 Coding Agent 开始开发**，我建议先改几个地方，否则后面很容易出现“看起来是 Agent，实际上是 LLM + 一堆固定调用”的问题。

### 1. 最大的问题：`search_stock` 的调用链要明确

例如：

> “我买了平安银行，现在能卖吗？”

LLM 首先不知道“平安银行”的代码。

所以应该允许：

```text
用户
 ↓
Agent
 ↓
search_stock("平安银行")
 ↓
得到 sz000001
 ↓
get_position("sz000001")
 ↓
get_stock_quote("sz000001")
 ↓
calculate_indicators(...)
 ↓
分析
```

这一点你的设计已经隐含了，但建议明确写成**Agent 必须先解决股票实体，再调用依赖 symbol 的 Tool**。

否则 Coding Agent 可能把股票名称解析硬编码进去。

------

### 2. `get_account` 和 `get_position` 的职责有点重复

你现在：

> `get_account`：各持仓行

同时：

> `get_position`：指定股票详细批次

我认为这样没问题，反而是合理的：

- `get_account()` → **快速了解整个账户**
- `get_position(symbol)` → **深入调查某一只股票**

可以保留。

------

### 3. S6 是最能体现 Agent 的场景之一

你写：

> “我现在持有的股票哪些风险比较大？”

这个非常好。

因为它不是简单：

```text
用户指定股票
→ 分析
```

而是：

```text
用户问题
 ↓
Agent发现需要全部持仓
 ↓
get_account()
 ↓
发现：
平安银行
比亚迪
贵州茅台
 ↓
逐只调用行情/K线/指标
 ↓
比较风险
 ↓
输出排序
```

这就是很明显的 Agent 行为。

不过你目前写了**单轮最多 Tool 调用 5 次**。

假设用户有 5 只股票：

```text
get_account
+ 5 × calculate_indicators
+ 5 × quote
```

很容易超过 5 次。

所以这里建议把：

> **工具调用 ≤5 次/轮**

改成：

> **Agent 最多 8～12 个 tool call/轮，单个 Tool 本身可以批量处理数据。**

更好的方案是增加：

```text
get_portfolio_risk_snapshot()
```

一次返回所有持仓的：

- 当前价格
- 盈亏
- MA
- RSI
- MACD
- 波动率

这样比让 Agent 自己循环调用更稳定、更省 Token。

------

### 4. 你现在“不做 RAG”是正确的

你文档最后明确：

> 第一阶段不做复杂 RAG。

**我赞成。**

第一版真正应该突出的是：

```text
LLM
+
Tool Calling
+
用户私有数据
+
实时股票数据
+
程序计算指标
+
多轮上下文
```

而不是强行加向量数据库。

以后如果你想升级，可以把你现有的：

```text
content/
├── terms.json
└── lessons/*.md
```

做成 RAG 知识库，再让 Agent 在用户问：

> “MACD 到底怎么看？”

或者：

> “为什么 RSI 过高意味着什么？”

的时候检索课程知识。

这时候 RAG 才真正有意义。

------

## 5. 我建议你再增加一个非常重要的 Tool

### `get_market_context()`

因为你现在的 Agent 分析个股时主要是：

> 个股行情 + K线 + 技术指标

但股票走势很大程度还和大盘环境有关。

例如：

> “现在适合买平安银行吗？”

最好能知道：

```text
上证指数
深证成指
创业板指
```

目前你的系统本身已经有：

```text
GET /api/market/indices
```

所以完全可以直接封装成：

```text
get_market_context()
```

返回：

```json
{
  "indices": [
    {
      "name": "上证指数",
      "changePct": 0.82
    },
    {
      "name": "深证成指",
      "changePct": 1.13
    },
    {
      "name": "创业板指",
      "changePct": 1.56
    }
  ]
}
```

这样 Agent 的分析会更像一个真正的“投资教练”，而不是只看一只股票的 K 线。

你现有架构本身就已经有三大指数接口，所以这是**低成本、高收益的增强**。

------

# 6. 还有一个我比较担心的地方：`decision`

你现在设计：

```text
buy
sell
hold
watch
split_sell
```

这个没问题。

但**不要让 score 变成一个看起来非常科学、实际上没有依据的数字。**

比如：

> 78/100

用户很容易理解成：

> AI 算出来“78分”，所以可以买。

实际上 LLM 并没有一个经过验证的量化模型。

我建议第一版把它叫：

> **综合参考分**

并明确：

> 该分数为 AI 基于当前获取信息生成的参考指标，不代表收益概率。

甚至第一版完全可以**不显示 score**，只显示：

```text
综合判断：偏向持有
置信度：中等
```

我个人更推荐这个。

------

# 7. 你的 `facts` 设计非常重要，要保留

这是我比较认可你文档的一点。

你要求：

```json
"facts": {
    ...
}
```

然后：

> Agent 先查事实，再判断。

这实际上是在解决 AI 股票应用里一个非常重要的问题：

**幻觉。**

例如：

用户：

> 我买平安银行成本是多少？

系统应该：

```text
Tool
↓
10.20
↓
LLM
↓
“你的持仓成本为 10.20 元”
```

而不是：

```text
LLM：根据你的情况，你大约是在10元左右买入……
```

所以你这个：

> **事实层 → 推理层**

一定要坚持。

------

# 最后，我建议你把项目架构最终定成这样

```text
                    用户
                     │
                     ▼
              自然语言 AI Agent
                     │
              意图 + 实体识别
                     │
                     ▼
             ┌── Tool Calling ──┐
             │                   │
             ▼                   ▼
       用户数据 Tools          市场数据 Tools
             │                   │
       ┌─────┼─────┐       ┌─────┼──────┐
       ▼     ▼     ▼       ▼     ▼      ▼
     账户   持仓   交易     行情   K线    大盘
                           │
                           ▼
                    技术指标计算
                           │
                           ▼
                    ┌───────────┐
                    │   LLM     │
                    │ 综合分析  │
                    └─────┬─────┘
                          │
                    Structured JSON
                          │
                          ▼
                    AI 教练 UI
```

未来再升级：

```text
                  Agent
                    │
          ┌─────────┴─────────┐
          ▼                   ▼
     Tool Calling             RAG
          │                   │
    实时/私有数据          学习知识库
```

**所以你现在这份设计，我会给 8.5/10，可以开始做。**

但在让 Coding Agent 开工前，我会先让它把 **①股票实体解析、②Tool 调用策略、③S6 多持仓批量分析、④大盘 Context、⑤事实与 AI 判断分离**这五点补进去。

尤其是第 ③ 点，否则你实际一问：

> “我持有的这些股票哪些风险比较大？”

Agent 很可能疯狂调用工具，最后触发你设定的 5 次上限。