# 复制为 .env 后填写(cp .env.example .env);.env 已被 git 忽略,严禁提交(密钥红线)

# ---- 网页访问口令(Basic Auth,浏览器首次打开会弹登录框)----
AUTH_USER=admin
AUTH_PASS=请改成强密码

# ---- 智谱 AI Investment Agent(留空则 AI 功能提示未配置,其余功能不受影响)----
ZHIPU_API_KEY=

# 可选:模型,默认 glm-5.3-flash;免费备选 glm-4.7-flash,最强 glm-5.3
# AI_MODEL=glm-5.3-flash
